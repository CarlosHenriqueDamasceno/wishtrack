version: "3.7"
services:
  database:
    image: postgres:17.4-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: wishtrack
    volumes:
      - database:/var/lib/postgresql/data
volumes:
  database:
